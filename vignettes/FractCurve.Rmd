---
title: "FractCurve: an R package for finding a point of fracture on a curve."
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{FractCurve: an R package for finding a point of fracture on a curve.}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(FractCurve)
```

# The concept of the algorithm.

```{r label, out.width = "85%", fig.cap = "A walkthrough of analysis concept."}
require(knitr)
include_graphics("Finding_a_point_of_fracture.pdf")
```


# The main algorithm.

The KM curve is scaled to ...

The function `fract_curve()` employ this analysis ...

# 2 conditions to be met the algorithm works.

## Straightness

## Convex to right

We confirmed that ...


